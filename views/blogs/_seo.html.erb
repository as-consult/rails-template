<% content_for :meta_title, "#{@blog.title} | #{meta_product}" %>
<% content_for :meta_description, "#{@blog.title}" %>
<% if @blog.picture.attached? %>
  <% content_for :meta_image, url_for(@blog.picture.variant(:thumb_big)) %>
<% end %>
